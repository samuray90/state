import styled from "styled-components";

export const StyledAplication = styled.div`
  display: content;
  justify-content: center;
  align-items: center;
  background-image: linear-gradient(red, purple, green);
`;
